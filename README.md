# Create project for executable

$ cargo new your_project_name --bin

$ cd your_project_name

$ vi src/main.rs

# Build debug executable

$ cargo build

# Run debug executable

$ cargo run

# Build release executable

$ cargo build --release

# Run debug executable

$ cargo run --release
